---
summary: "@cyanheads/mcp-ts-core ^0.9.21 — per-request log context fix, fetchWithTimeout secret scrubbing, withRetry fail-fast; vitest ^4.1.8; release:github script; skill sync"
breaking: false
security: false
---

# 0.1.11 — 2026-06-02

## Changed

- **`@cyanheads/mcp-ts-core`** ^0.9.16 → ^0.9.21 — HTTP transport per-request log context fix (logs and traces carry fresh request + trace/span IDs instead of the frozen boot context); `fetchWithTimeout` strips query-string secrets (e.g. `?api_key=`) from error messages and logs; `withRetry` fails fast on non-retryable errors; `ctx.fail` auto-populates the `retryable` flag.
- **`vitest`** ^4.1.7 → ^4.1.8 (dev).
- **`scripts/devcheck.ts`** — re-synced to framework ^0.9.21 conventions.
- **`scripts/release-github.ts`** — new script; wires `bun run release:github` for one-command GitHub Release creation with `.mcpb` attachment.
- **`scripts/check-skill-versions.ts`** — new framework-sourced script added by maintenance sync.
- **Skills** (`add-tool`, `add-service`, `api-canvas`, `api-context`, `api-linter`, `api-utils`, `design-mcp-server`, `release-and-publish`) — re-synced to framework ^0.9.21.
- **`skills/api-mirror`** — new skill added by framework sync (MirrorService reference).
- **`skills/orchestrations`** — new skill added by framework sync.
- **README** — client-config server key renamed to full package name (`earthquake-mcp-server`).
- **`.claude-plugin/plugin.json`**, **`.codex-plugin/mcp.json`** — manifest args simplified.
